[Unit]
Description=Meilisearch
After=systemd-user-sessions.service

[Service]
User=root
Type=simple
ExecStart=/usr/local/bin/meilisearch --http-addr 127.0.0.1:7700 --env production --master-key "aSampleMasterKey"
Restart=always

[Install]
WantedBy=multi-user.target
